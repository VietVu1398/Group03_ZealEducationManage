@model IEnumerable<ZealEducationManager.Entities.Batch>

@{
    ViewData["Title"] = "Batches";
    Layout = "~/Views/Shared/Admin_Layout.cshtml";
}



<div class="page-header">
	<div class="row">
		<div class="col-lg-6 col-md-6 col-sm-6 col-12">
			<h5 class="text-uppercase mb-0 mt-0 page-title">Batches</h5>
		</div>
	</div>
</div>
<div class="content-page">
	<div class="row">
		<div class="col-sm-5 col-12">
		</div>
		<div class="col-sm-7 col-12 text-right add-btn-col">
			<a asp-action="Create" class="btn btn-primary btn-rounded" >
				<i class="fas fa-plus"></i> Add New Batch
			</a>
		</div>
	</div>
	<div class="row">
		<div class="col-md-12 mb-3">
			<div class="table-responsive">
				<table class="table custom-table mb-0 datatable">
					<thead class="thead-light">
						<tr>
							<th>Batch Code </th>
							<th>Start Date</th>
							<th>End Date</th>
							<th class="text-right">Action</th>
						</tr>
					</thead>
					<tbody>
						@foreach (var item in Model)
						{
							<tr class="Batch-completed">
								<td>
									@Html.DisplayFor(modelItem => item.BatchCode)
								</td>
								<td>
									@Html.DisplayFor(modelItem => item.StartDate)
								</td>
								<td>
									@Html.DisplayFor(modelItem => item.EndDate)
								</td>
								<td class="text-right">
									<div class="dropdown dropdown-action">
										<a href="#" class="action-icon dropdown-toggle" data-toggle="dropdown"
										   aria-expanded="false"><i class="fas fa-ellipsis-v"></i></a>
										<div class="dropdown-menu dropdown-menu-right">
											<a class="dropdown-item" asp-action="ListCandidate" asp-route-id="@item.BatchId" title="Detail">
												<i class="fas fa-book m-r-5"></i> List Candidates
											</a>
											<a class="dropdown-item" asp-action="Edit" asp-route-id="@item.BatchId"  title="Edit">
												<i class="fas fa-pencil-alt m-r-5"></i> Edit
											</a>
											<a class="dropdown-item" asp-action="Delete" asp-route-id="@item.BatchId"  title="Delete">
												<i class="fas fa-trash-alt m-r-5"></i> Delete
											</a>
										</div>
									</div>
								</td>
							</tr>
						}

					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>
